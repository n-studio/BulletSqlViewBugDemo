<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <%= render partial: "products/product", locals: { product: } %>
    <p>
      <%= link_to "Show this product", product_path(product.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New product", new_product_path %>
